#include<stdio.h>
#include<stdlib.h>

#define MAX_SIZE 10000
#define TRUE 1
#define FALSE 0;

unsigned long int array[MAX_SIZE];

int compare(const void *, const void *);

int main(void) {
    int size = 0;
    int i;
    while (scanf("%lu\n", &array[size++]) != EOF) {
    
    }

    return 0;
}

